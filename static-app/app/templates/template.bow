!doctype("html")
html {
  head {
    import "head.bow"
  }
  body {
    div.container {
      div.header.clearfix {
        nav {
          ul.nav.nav-pills.pull-right {
            li.active(role="presentation") { a(href="#"): "Home" }
            li(role="presentation") { a(href="#"): "About" }
            li(role="presentation") { a(href="#"): "Contact" }
          }
        }
        h3.text-muted: "Example app"
      }
      bowtie:content
    }
  }
}
